/**
 * Defines the DatabaseCompany contract.
 */
/**
 * @module types/database
 * Module implementation for database.ts.
 */

export interface DatabaseCompany {
  id: string;
  name: string;
  domain: string;
}
